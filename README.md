# Student_Registration_Form_React_MongoDB

CORS (Cross-Origin Resource Sharing) is a security mechanism built into web browsers.
It controls which websites are allowed to make requests to your backend.

CORS tells the browser:

“It’s safe. This domain is allowed to access my API.”

frontend:

npm create-react-app frontend

replace the src file

backend:

libraries need to install in backend server.js

npm init -y
npm install express mongoose cors